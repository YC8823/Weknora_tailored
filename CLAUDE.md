# 核心交互规则
- **语言要求**：始终使用**中文**（简体）与我进行交流、解释代码和撰写注释。

# 操作系统与终端规范
- **系统环境**：Windows 11。
- **终端环境**：默认使用 **PowerShell**。
- **命令规范**：
  - 所有提供的命令行操作必须严格符合 PowerShell 的语法和特性。
  - 例如：处理环境变量使用 `$env:VAR_NAME`，文件路径拼接使用 Windows 规范，换行符使用反引号 `` ` `` 而不是 `\`。
  - 避免提供仅能在 Bash/Linux 下运行的命令（如 `export`、`ls -la` 等，请转换为相应的 PS 等效命令或说明）。

# Python 与环境管理
- **工具栈**：项目使用 **Anaconda** 管理 Python 环境。
- **依赖安装策略**：
  - 在执行安装新依赖（特别是类似 PyTorch、CUDA Toolkit 等体积庞大的包）的建议或操作前，优先检查和利用 Conda 缓存/本地缓存（如使用 `--use-local` 或检查 `pkgs` 目录），避免重复进行耗时的网络下载。
  - 优先推荐 `conda install` 命令，若需使用 `pip`，请确保是在激活的虚拟环境中操作。
  - 在清理或重建环境时，注意保留大型下载缓存。

# 硬件限制与 CUDA 兼容性
- **GPU 规格**：NVIDIA RTX 5070 Ti，显存为 **16 GB**。
- **CUDA 版本硬性要求**：本机属于 RTX 50 系列架构，**必须使用 CUDA 12.8 或更高版本**才能正常运行和发挥性能。提供 PyTorch 等深度学习框架的安装指令时，严格匹配 CUDA >= 12.8 的构建版本。
- **显存与模型大小管理**：
  - 在推荐本地运行的开源大语言模型 (LLM)、视觉模型或其他深度学习任务时，必须结合 16GB 的显存上限进行评估。
  - 推荐模型时，请主动说明所需的量化版本（如 GGUF、AWQ、ExLlamaV2 的 4-bit / 8-bit 量化），并确保模型加载加上上下文（KV Cache）的峰值显存占用控制在安全范围内（建议不超过 14-15 GB，预留系统冗余）。

# 尊重项目原代码
- 当前项目由Tencent/WeKnora的github开源项目（https://github.com/Tencent/WeKnora）修改而来，由docker compose整体部署
- 在为项目定制新功能时的时候，遵循最小改动原则
- 在修改bug的时候，尽量不要更改底层代码的逻辑，如果需要修改，一定要向用户确认（造成bug的原因 + 需要改动的文件 + 改动理由），只有在用户统一修改后再实施改动，改动完成后自觉跟进改动文档（docs/改动记录.md）

# docker环境
- 每次改动如果涉及需要重启/重建docker的情况，在回答的最后简单说明。